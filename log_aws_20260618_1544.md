# AWS 배포 작업 로그 — 2026-06-18 (KST 15:44)

## 작업 환경
- 로컬: Windows 11 PowerShell + WSL (Ubuntu)
- 서버: AWS EC2 Ubuntu 26.04 LTS (`13.210.159.103`)
- EC2 Java: OpenJDK 21.0.11 (Temurin)
- 프로젝트: StockFor (`core-0.0.1-SNAPSHOT.jar`)

---

## 작업 순서

### 1. Java 21 설치 (로컬)
```
winget install EclipseAdoptium.Temurin.21.JDK
→ 설치 성공: C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot
```

### 2. JAVA_HOME 설정 시도 (실패)
```powershell
$env:JAVA_HOME = "C:\Program Files\Eclipse Adoptium\jdk-21.0.11.10-hotspot"
$env:Path = "$env:JAVA_HOME\bin;" + $env:Path
```
- `java -version` → 21.0.11 확인됨

### 3. 첫 번째 빌드: `./gradlew bootJar` — **성공**
```
cd core
./gradlew bootJar
→ BUILD SUCCESSFUL
→ 결과물: core/build/libs/core-0.0.1-SNAPSHOT.jar (약 54MB)
```
- 이 시점 JAVA_HOME은 세션 종료로 초기화, 실제 빌드는 Java 17로 수행됨

### 4. 두 번째 빌드 시도 — **실패**
```
JAVA_HOME = jdk1.8.0_202 (시스템 기본값이 JDK8 상태)
./gradlew bootJar
```
- 오류: `Spring Boot Gradle Plugin 3.3.7`이 Java 8 비호환
- `spring-boot-gradle-plugin:3.3.7`은 Java 17 이상 필요

### 5. Gradle Wrapper 업그레이드 시도 — **실패**
```
./gradlew wrapper --gradle-version 8.7
```
- 동일 원인(JAVA_HOME=JDK8)으로 실패

### 6. 세 번째 빌드 시도 — **실패**
```powershell
$env:JAVA_HOME = "C:\Program Files\Java\jdk-23"
./gradlew bootJar
```
- `java -version`이 여전히 17.0.7 반환 (PATH 우선순위 문제)
- 오류: `Unsupported class file major version 67`
  - class file version 67 = Java 23 컴파일 결과를 Java 17로 실행 시도한 충돌

> **결론**: 첫 번째 빌드(Java 17)에서 생성된 JAR를 사용해 배포 진행

### 7. EC2로 JAR 전송 — **성공**
```bash
# WSL 터미널에서
scp -i ~/.ssh/stockFor.pem \
  /mnt/c/Users/USER/Documents/GitHub/StockFor/core/build/libs/core-0.0.1-SNAPSHOT.jar \
  ubuntu@13.210.159.103:~/
→ 전송 성공 (53MB, 약 5초)
```

### 8. EC2 SSH 접속 — **성공**
```bash
ssh -i ~/.ssh/stockFor.pem ubuntu@13.210.159.103
→ Ubuntu 26.04 LTS, Java 21.0.11 환경 확인
```

### 9. EC2에서 JAR 실행 — **실패 (앱 기동 불가)**
```bash
java -jar ~/core-0.0.1-SNAPSHOT.jar
```

Spring Boot 기동 중 두 가지 문제 감지:

#### 문제 1 (경고, 비치명적)
```
LibraryNotFoundError: tcnative 라이브러리 없음
```
- Tomcat Native(APR) 라이브러리 미설치
- 기능에는 영향 없음 (fallback으로 Java NIO 사용)

#### 문제 2 (치명적) — 앱 기동 실패 원인
```
BeanCreationException: Ambiguous mapping.
Cannot map 'kiwoomETFController' method
  KiwoomETFController#EtfHourlyContractController(String)
to {POST [/api/etf/hourly-trade]}:
There is already 'kiwoomETFController' bean method
  KiwoomETFController#EtfHourlyTrendController(String) mapped.
```
- `KiwoomETFController`에서 두 메서드가 동일 URL에 중복 매핑됨
- `EtfHourlyTrendController` (ka40009)
- `EtfHourlyContractController` (ka40010)
- 두 메서드 모두 `POST /api/etf/hourly-trade`로 등록되어 Spring 기동 시 충돌

---

## 현재 상태

| 항목 | 상태 |
|---|---|
| JAR 빌드 | ✅ 완료 (Java 17, `core-0.0.1-SNAPSHOT.jar`) |
| EC2 JAR 업로드 | ✅ 완료 (`/home/ubuntu/core-0.0.1-SNAPSHOT.jar`) |
| EC2 앱 기동 | ❌ 실패 |

---

## 다음 필요 작업

1. **[긴급] 중복 URL 매핑 수정**
   - 파일: `KiwoomETFController.java`
   - `EtfHourlyContractController` 메서드의 URL을 `/api/etf/hourly-contract` 등으로 분리
   - 대응 TR: `ka40009` vs `ka40010` 구분 필요

2. 수정 후 재빌드 → EC2 재배포

3. (선택) `application.properties` 환경변수화 후 EC2에 설정

4. (선택) `systemd` 서비스 등록으로 자동 재시작 설정
