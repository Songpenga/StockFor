# X-API-KEY 헤더 유출 가능성 정리

> 작성일: 2026-06-25  
> 맥락: 키움증권 REST API Spring Boot 서버의 `/api/trade/**` 엔드포인트에  
> X-API-KEY 인터셉터 인증을 도입할 때 보안 검토한 내용

---

## 질문

매수/매도 요청 헤더에 `X-API-KEY`를 넣으면, 그 키가 유출될 수 있지 않을까?

---

## 유출될 수 있는 경우

### 1. HTTPS 미적용 (HTTP 평문 통신)

```
클라이언트 → [평문 전송] → 서버
헤더에 X-API-KEY: 내-비밀키 가 그대로 노출
```

중간에서 패킷 스니핑하면 헤더 내용이 그대로 보인다.  
→ **근본 해결책: HTTPS 적용**

### 2. 브라우저 프론트엔드에서 직접 호출

```javascript
fetch('/api/trade/buy', {
  headers: {
    'X-API-KEY': '내-비밀키'  // 누구나 DevTools Network 탭에서 볼 수 있음
  }
})
```

JS 소스코드나 브라우저 개발자도구 네트워크 탭에서 키가 그대로 노출된다.  
→ **해결책: 프론트엔드에서 직접 키를 들고 호출하지 않도록 설계 (BFF 패턴 등)**

### 3. 서버 로그에 DEBUG 레벨로 헤더가 찍히는 경우

```
DEBUG - Request Headers: X-API-KEY=내-비밀키  ← app.log에 기록됨
```

로그 파일이 유출되면 키도 같이 유출된다.  
→ **해결책: `logging.level.root=WARN` 으로 변경하여 헤더 로깅 차단**

---

## 유출되지 않는 경우

**서버 → 서버 통신 + HTTPS 적용 시**

- Postman이나 백엔드 서버에서만 키를 보관하고 호출
- HTTPS로 전송 구간이 암호화되어 중간 도청 불가
- 키가 프론트 코드나 클라이언트에 없으므로 노출 경로 없음

---

## 이 프로젝트에서의 현실적인 구조

본인만 사용하는 서버이므로 아래 구조면 충분히 안전하다:

```
본인 PC (키 보관) → HTTPS → EC2 서버
```

---

## 보안 적용 우선순위

| 순서 | 작업 | 효과 |
|------|------|------|
| 1 (즉시) | `logging.level.root=DEBUG` → `WARN` | 토큰/키가 로그에 찍히는 것 차단 |
| 2 (단기) | X-API-KEY 인터셉터 구현 | 앱 레벨 인증 (인증 없으면 403 반환) |
| 3 (중기) | HTTPS 적용 (nginx + Let's Encrypt) | 전송 구간 암호화 |

---

## 결론

X-API-KEY는 "완벽한 보안"이 아니라,  
**아무 인증도 없는 현재 상태보다 훨씬 나은 1차 방어선**이다.

HTTPS 없이 쓰면 전송 구간이 평문이라는 한계가 있지만,  
서버-투-서버 + HTTPS 조합에서는 충분히 실용적인 인증 방식이다.
