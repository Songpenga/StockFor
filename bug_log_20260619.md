# 버그 수정 로그 — 2026-06-19

## 파일: `KiwoomTradingController.java`

---

## Bug 1 — 정정주문: `@PathVariable`에 DTO 바인딩 시도

### 발견 경위
키움 REST API PDF 문서(kt10002)와 코드 비교 검증 중 발견

### 버그 내용
```java
// 수정 전
@PostMapping("/order/{orderNo}")
public ResponseEntity<?> editOrder(@PathVariable editOrderRequestDto request)
```

- `@PathVariable`은 URL 경로 변수를 `String`, `int` 등 단순 타입으로만 바인딩 가능
- `editOrderRequestDto` 같은 객체 타입은 바인딩 불가 → 호출 시 런타임 에러 발생
- PDF 스펙(kt10002) 확인 결과: `orig_ord_no`를 포함한 모든 파라미터가 Request Body에 위치
- URL에 `{orderNo}` 경로변수를 두는 설계 자체가 API 스펙과 불일치

### 수정 내용
```java
// 수정 후
@PostMapping("/edit")
public ResponseEntity<?> editOrder(@RequestBody editOrderRequestDto request)
```

| 항목 | 수정 전 | 수정 후 |
|---|---|---|
| URL | `/api/trade/order/{orderNo}` | `/api/trade/edit` |
| 파라미터 바인딩 | `@PathVariable` | `@RequestBody` |

---

## Bug 2 — 취소주문: `@PathVariable` 오용 + 잘못된 HTTP 메서드

### 발견 경위
키움 REST API PDF 문서(kt10003)와 코드 비교 검증 중 발견

### 버그 내용
```java
// 수정 전
@DeleteMapping("/order/{orderNo}")
public ResponseEntity<?> cancelOrder(@PathVariable cancelOrderRequestDto request)
```

- `@PathVariable`에 DTO 바인딩 불가 — Bug 1과 동일한 원인으로 런타임 에러 발생
- PDF 스펙(kt10003) 확인 결과: HTTP Method = **POST** → `@DeleteMapping` 사용은 스펙 위반
- 키움 취소주문 API는 RESTful DELETE가 아닌 POST + Body 방식으로 동작

### 수정 내용
```java
// 수정 후
@PostMapping("/cancel")
public ResponseEntity<?> cancelOrder(@RequestBody cancelOrderRequestDto request)
```

| 항목 | 수정 전 | 수정 후 |
|---|---|---|
| HTTP 메서드 | `@DeleteMapping` | `@PostMapping` |
| URL | `/api/trade/order/{orderNo}` | `/api/trade/cancel` |
| 파라미터 바인딩 | `@PathVariable` | `@RequestBody` |

---

## 수정 후 최종 엔드포인트 정리

| TR | 기능 | HTTP | URL |
|---|---|---|---|
| kt10000 | 매수주문 | POST | `/api/trade/buy` |
| kt10001 | 매도주문 | POST | `/api/trade/sell` |
| kt10002 | 정정주문 | POST | `/api/trade/edit` |
| kt10003 | 취소주문 | POST | `/api/trade/cancel` |
