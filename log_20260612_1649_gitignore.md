# .gitignore 설정 - 학습 기록

**날짜**: 2026-06-12 16:49

---

## Q. .gitignore 설정을 하는 이유와 하는 방법을 알려줄래?

### 왜 필요한가?

git은 기본적으로 **모든 파일을 추적**합니다. 그런데 이런 파일들은 GitHub에 올라가면 안 됩니다:

- `application.properties` — API 키, DB 비밀번호 등 민감한 정보
- `kiwoom_api.yml` — 키움증권 API 키
- `.idea/` — 내 컴퓨터 IDE 설정 (남한테 필요 없음)

`.gitignore`는 **"이 파일들은 git이 신경 쓰지 마"** 라고 지정하는 파일입니다.

### 설정 방법

1. 프로젝트 루트에 `.gitignore` 파일 생성 (없으면 새로 만들기)
2. 제외할 파일/폴더명을 한 줄씩 작성

```
# 민감한 설정 파일
application.properties
kiwoom_api.yml

# IDE 설정
.idea/

# 빌드 결과물
build/
*.jar
```

3. `.gitignore` 파일 자체는 git에 추가하고 커밋

### 주의: 이미 git에 올라간 파일 제거

실수로 이미 커밋한 파일은 `.gitignore`만으로는 제거되지 않습니다.
아래 명령어로 git 추적에서 제거해야 합니다:

```bash
git rm --cached application.properties
git commit -m "remove sensitive file from tracking"
```

---

## Q. AI가 도와주더라도 이런 걸 알고 있는 게 나을까?

**네, 알고 있는 게 훨씬 낫습니다.** 이유는 세 가지입니다:

**1. AI는 맥락을 모릅니다**
- "왜 이걸 해야 하는지" 모르면 AI가 틀린 답을 줘도 걸러낼 수 없습니다

**2. 실수를 본인이 막아야 합니다**
- 키값을 GitHub에 올리는 실수는 AI가 막아주지 않습니다. 본인이 인식하고 있어야 합니다

**3. 면접/협업에서 드러납니다**
- "`.gitignore` 왜 씁니까?" 같은 기본 질문은 AI 없이 답해야 합니다

> AI는 **속도를 높여주는 도구**지, **이해를 대신해주는 도구가 아닙니다.**
> 개념을 알고 AI를 쓰는 것과 모르고 쓰는 것은 결과물의 품질이 다릅니다.

---

## 핵심 요약

| 항목 | 내용 |
|---|---|
| `.gitignore` 목적 | 민감한 파일을 git 추적에서 제외 |
| 주요 제외 대상 | API 키, 비밀번호, IDE 설정, 빌드 파일 |
| 이미 올라간 파일 제거 | `git rm --cached <파일명>` |
| AI 활용 원칙 | 개념은 직접 이해, AI는 속도 도구로 활용 |
